_base_: maplegrasp_v3_stage2.yaml

TRAIN:
  # Stage 1 is segmentation-only and remains a valid initializer.  The grasp
  # head itself starts fresh in this new experiment directory.
  exp_name: maplegrasp_stage2_grasp_tools_v3_15k_unified_original300
  resume:

TEST:
  evaluation_protocol: toolrgs
  grasp_topk: [1, 5]
  grasp_iou_threshold: 0.25
  grasp_angle_threshold: 30.0
  restore_grasp_size_scale: False
