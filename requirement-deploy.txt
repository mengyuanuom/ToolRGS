# Install requirement.txt and the correct CUDA/PyTorch build first.
PyQt5==5.15.11
pyrealsense2==2.56.5.9235
sounddevice==0.5.1
openai-whisper==20240930
# torchvision is required only when gelsight.enabled=true. Install the build
# matching the existing torch/CUDA environment instead of forcing a wheel here.
